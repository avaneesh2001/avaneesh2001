---
import type { FooterLink, SiteContent } from "../types/content";
import { toRelativeUrl } from "@lib/paths";

interface Props {
  profile: SiteContent["profile"];
  links: FooterLink[];
  currentPath: string;
}

const { profile, links, currentPath } = Astro.props;
---

<footer class="site-footer">
  <div>
    <p class="footer-kicker">Document archive</p>
    <p>{profile.name} · {profile.email}</p>
  </div>
  <ul class="footer-links" aria-label="Contact links">
    {links.map((link) => (
      <li><a href={toRelativeUrl(currentPath, link.href)}>{link.label}</a></li>
    ))}
  </ul>
</footer>
