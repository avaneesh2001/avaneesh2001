import { defineConfig } from "astro/config";

export default defineConfig({
  output: "static",
  trailingSlash: "always",
  site: "https://avaneesh2001.github.io/avaneesh2001/",
  base: '/avaneesh2001/'
});
